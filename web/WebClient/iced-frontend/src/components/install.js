// components/Install.js (예시)
import React from 'react';

const Install = () => {
    return (
        <div>
            <h2>클라이언트 설치</h2>
            {/* 파일 업로드 기능 구현 */}
        </div>
    );
};

export default Install;
